git clone https://github.com/<ID098>/relaxa-protocol-token.git
cd relaxa-protocol-token

# Remove the placeholder README, Node .gitignore & license (we’ll add our own)
rm -f README.md .gitignore LICENSE

